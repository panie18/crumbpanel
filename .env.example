# Database Configuration
POSTGRES_USER=mc_admin
POSTGRES_PASSWORD=changeme_secure_db_password
POSTGRES_DB=mc_panel
DATABASE_URL=postgresql://mc_admin:changeme@db:5432/mc_panel

# Backend Configuration
PORT=5829
NODE_ENV=production
JWT_SECRET=changeme_min32chars_very_secure_key_here
JWT_REFRESH_SECRET=changeme_different_32chars_key
JWT_EXPIRE=1h
JWT_REFRESH_EXPIRE=7d
ENCRYPTION_KEY=exactly16charss  # Must be exactly 16 characters

# Frontend Configuration
VITE_API_URL=http://localhost:5829
VITE_WS_URL=ws://localhost:5829

# Admin Account Setup
# NOTE: These are IGNORED if a user already exists in the database!
# The first admin account is created through the Setup Wizard in the browser.
# These values are only used as fallback if database is empty.
DEFAULT_ADMIN_EMAIL=admin@crumbpanel.local
DEFAULT_ADMIN_PASSWORD=please_change_me_123

# Optional: WebDAV Cloud Backup
# WEBDAV_URL=https://your-nextcloud.com/remote.php/dav/files/username/
# WEBDAV_USERNAME=your_username
# WEBDAV_PASSWORD=your_app_password
# WEBDAV_REMOTE_PATH=/minecraft-backups
